services:
  web:
    build:
      context: ../../../apps/web
      dockerfile: ../../infra/web/docker/Dockerfile
    image: ai-assitant-web:local
    env_file:
      - web.env
    ports:
      - "80:80"
    extra_hosts:
      - "host.docker.internal:host-gateway"
